global:
  scrape_interval: 10s

scrape_configs:
  - job_name: wisteria
    static_configs:
      - targets: [localhost:3000]
    basic_auth:
      username: example-wisteria-user
      password: example-wisteria-pass
